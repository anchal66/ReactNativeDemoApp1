import React from 'react'
import { View, StyleSheet, Text, TextInput } from 'react-native';

export default ResultDetails = (props) => {
    return <View>
        <Text>{props.restruant.name}</Text>
    </View>
}
const styles = StyleSheet.create({
})